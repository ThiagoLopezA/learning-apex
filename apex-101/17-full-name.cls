/**
 Given two non-empty strings firstName and lastName, return the name as 
 a single string with a space in between (firstName lastName).

 formatName('Jane', 'Doe') = 'Jane Doe'
*/

public class Result {
    public String formatName(String firstName, String lastName) {
        return firstname + ' ' + lastName;
    }
}